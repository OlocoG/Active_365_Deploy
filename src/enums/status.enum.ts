export enum statusAppointment {
    active = "active",
    cancelled = "cancelled"
}

export enum statusGym {
    active = "active",
    inactive = "inactive",
}

export enum statusProduct {
    active = "active",
    inactive = "inactive",
}

export enum statusUser {
    active = "active",
    inactive = "inactive",
}

